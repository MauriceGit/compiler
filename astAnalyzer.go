package main

// annotateTypes traverses the tree and annotates variables and expressions with their corresponding type!
// returns an error if we have a type missmatch anywhere!
func annotateTypes(ast AST) AST {
	return ast
}
